{{#if sources}}
  <ul>
    {{#each sources}}
      <li{{#equals status "deleted"}} class="ul-deleted"{{/equals}}>
          <a href="/api/product/{{source}}/{{sid}}" target="_blank">
              {{source}} : {{name}}
          </a>
      </li>
    {{/each}}
  </ul>
{{/if}}